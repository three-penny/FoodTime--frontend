# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-25 18:03:07
- **提交者**：naph130
- **Commit Hash**：61587b7

## 提交信息
feat: 菜品上传标签改为交互式录入并优化审核台展示

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/views/submission/DishUploadView.vue | 修改 | 标签输入改为 + 按钮交互式录入，含确认/取消/删除 |
| src/views/admin/AdminAuditView.vue | 修改 | 审核卡片标签与图片分离为独立两栏 |
| src/views/profile/ProfileView.vue | 修改 | 删除兑换优惠券按钮 |

## 变更详情
1. DishUploadView 标签模块：移除逗号分隔文本输入，改为 tag chip 列表 + 新增按钮，支持逐个录入含确认/取消、重复校验、Enter/Esc 快捷键。
2. AdminAuditView 菜品待审卡片：标签栏不再混入图片文件名，图片独立一行展示。
3. ProfileView 积分明细：移除"兑换优惠券"按钮及对应 handleExchange 方法。

## 影响范围
- 影响模块：菜品上传表单、管理员审核台、个人中心
- 向前兼容：是，提交的 tags 字段格式不变（逗号分隔字符串）

## 合并冲突处理指引
- 若 DishUploadView.vue 标签区域冲突，保留 tag-chip 交互模式版本。
- 若 AdminAuditView.vue 冲突，确保图片脱离 itemTags 单独渲染。
