# Git 提交日志

## 基本信息
- **分支名**：develop
- **提交时间**：2026-06-01 11:03:06
- **提交者**：NAPHthalene130
- **Commit Hash**：8353ed3

## 提交信息
ci: 添加 GitHub Actions CI 工作流

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| .github/workflows/ci.yml | 新增 | GitHub Actions CI 配置 |

## 变更详情
1. 新增 `.github/workflows/ci.yml`，包含三个并行 job：
   - `lint`: ESLint + Prettier 检查（未配置时自动跳过不阻塞）
   - `test`: `vitest run` 执行单元测试
   - `build`: `vite build` 构建验证
2. 触发条件：push / pull_request 到 `develop` 和 `main` 分支

## 影响范围
- 影响模块：CI/CD
- 向前兼容：是
