# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-27 20:00:00
- **提交者**：opencode
- **Commit Hash**：TBD

## 提交信息
feat: 前端首页今日推荐/本周热榜改为对接后端推荐 API

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/api/canteen.api.js | 修改 | 新增 fetchDailyRecommendations / fetchWeeklyRecommendations |
| src/store/useCanteenStore.js | 修改 | 新增 dailyRecommendations / weeklyRecommendations 状态和加载方法 |
| src/views/home/HomeView.vue | 修改 | 今日推荐改为 GET /recommendations/daily；本周热榜改为 GET /recommendations/weekly |

## 变更详情
1. canteen.api.js：新增两个推荐端点调用。
2. canteenStore：
   - 新增 dailyRecommendations / weeklyRecommendations 响应式状态。
   - loadDailyRecommendations：加载每日推荐并附加 image/comment 字段。
   - loadWeeklyRecommendations：加载每周推荐并附加 rank/stamp/image/valueNote 等字段。
3. HomeView：
   - recommendations 计算属性改从 canteenStore.dailyRecommendations 读取。
   - rankings 计算属性改从 canteenStore.weeklyRecommendations 读取。
   - 移除旧客户端过滤逻辑（rating >= 4.7 取前 6 条）。
   - 移除旧的 rankings 手动富化逻辑。
   - onMounted 新增 loadDailyRecommendations / loadWeeklyRecommendations 调用。

## 影响范围
- 影响模块：首页 01 今日推荐、03 本周热榜
- 影响接口：GET /recommendations/daily、GET /recommendations/weekly
- 向前兼容：是

## 合并冲突处理指引
- 若 canteenStore 冲突，确保包含 dailyRecommendations 和 weeklyRecommendations 的状态和 actions。
- 若 HomeView 冲突，确保 recommendations 和 rankings 使用 canteenStore 的新字段。
