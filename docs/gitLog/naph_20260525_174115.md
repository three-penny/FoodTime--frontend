# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-25 17:41:15
- **提交者**：naph130
- **Commit Hash**：3ffed78

## 提交信息
fix: 修复管理员注册邀请码与后端配置对齐

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/store/useAuthStore.js | 修改 | ADMIN_INVITE_CODE 改为 ABCDEF 对齐后端默认值 |
| src/views/auth/RegisterView.vue | 修改 | role=admin 时传递 invite_code 参数 |

## 变更详情
1. `useAuthStore.js` 中 `ADMIN_INVITE_CODE` 从 `FT-ADMIN-2026` 改为 `ABCDEF`，与后端 `config.py` 中 `ADMIN_INVITE_CODE` 默认值一致。
2. `RegisterView.vue` 注册请求增加 `invite_code` 字段，当 `role === 'admin'` 时传递用户输入的邀请码。

## 影响范围
- 影响模块：管理员注册
- 影响接口：POST /api/v1/auth/register
- 向前兼容：是，普通用户注册流程不受影响

## 合并冲突处理指引
- 若 `useAuthStore.js` 发生冲突，确保 `ADMIN_INVITE_CODE` 值与后端 config 中 `ADMIN_INVITE_CODE` 一致。
- 若 `RegisterView.vue` 发生冲突，确保注册请求体包含 `invite_code` 字段。
