# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-25 17:37:18
- **提交者**：naph130
- **Commit Hash**：a13ead4

## 提交信息
fix: 修复投稿审核驳回API未调用及管理员加载全部投稿BUG

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/api/submission.api.js | 修改 | 新增 fetchAllSubmissions 管理员专用接口 |
| src/store/useSubmissionStore.js | 修改 | 删除重复 rejectSubmission、新增 loadAllSubmissions action |
| src/views/admin/AdminAuditView.vue | 修改 | 修复加载投稿改用 loadAllSubmissions |
| rules.md | 修改 | 新增 4.5 Git提交日志记录规范 |

## 变更详情
1. 删除 `useSubmissionStore.js` 中重复定义的 `rejectSubmission` 方法（纯本地更新版本覆盖了异步 API 调用版本），导致管理员驳回操作从未写入后端。
2. 新增 `fetchAllSubmissions()` API 函数（GET /submissions 不带 account 参数）。
3. 新增 `loadAllSubmissions()` store action，管理员审核台 `onMounted` 改为调用此方法，修复传入 `account='all'` 导致查询不到数据的问题。
4. rules.md 新增 4.5 Git提交日志记录规范，要求每次提交在 docs/gitLog/ 创建 `<BranchName>_<Time>.md` 日志。

## 影响范围
- 影响模块：管理员审核台、用户投稿列表
- 影响接口：GET /api/v1/submissions、PUT /api/v1/submissions/:id/audit
- 向前兼容：是，普通用户投稿流程不受影响

## 合并冲突处理指引
- 若 `useSubmissionStore.js` 发生冲突，确保只保留一个 `rejectSubmission`（异步 API 调用版本），同时保留 `loadAllSubmissions` 和对应的 import。
- 若 `AdminAuditView.vue` 发生冲突，确保 `onMounted` 中调用 `loadAllSubmissions()` 而非 `loadSubmissions('all')`。
- 若 `submission.api.js` 发生冲突，保留 `fetchAllSubmissions` 导出。
