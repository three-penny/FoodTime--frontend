# Git 提交日志

## 基本信息
- **分支名**：develop
- **提交时间**：2026-06-01 11:17:47
- **提交者**：NAPHthalene130
- **Commit Hash**：e3f6f79

## 提交信息
feat(superadmin): 用户管理表增加改密码按钮

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/api/superadmin.api.js | 修改 | 新增 changeUserPassword API |
| src/views/superadmin/SuperadminView.vue | 修改 | 用户操作列增加改密码按钮和逻辑；操作日志筛选项增加密码重置 |

## 变更详情
1. `superadmin.api.js` 新增 `changeUserPassword(userId, password)` 调用后端接口
2. `SuperadminView.vue` 用户表格每行新增「改密码」按钮，点击后 prompt 输入新密码
3. 操作日志筛选项和标签增加 `password_change`（密码重置）

## 影响范围
- 影响模块：超级管理员控制台（用户管理标签页）
- 向前兼容：是
