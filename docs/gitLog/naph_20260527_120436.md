# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-27 12:04:36
- **提交者**：opencode
- **Commit Hash**：TBD

## 提交信息
feat: 管理员审核台新增评论审核 + 完善点评投稿档口选择

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/api/review.api.js | 修改 | 新增 fetchAllReviews / auditReview |
| src/store/useReviewStore.js | 新增 | 评论审核状态管理 |
| src/views/admin/AdminAuditView.vue | 修改 | 新增评论审核 Tab，显示 nickname 和餐厅-档口-菜品路径 |
| src/views/review/ReviewCreateView.vue | 修改 | 选择餐厅后通过 API 加载该餐厅全部档口和菜品 |

## 变更详情
1. AdminAuditView：
   - 新增"评论审核"审核类型，与菜品投稿、吐槽墙并列。
   - 评论卡片展示：评分星级、评论者昵称、餐厅-档口-菜品路径、驳回理由。
   - 支持通过/驳回操作，审核状态同步后端。
2. useReviewStore：新增评论审核状态管理（loadAllReviews / approveReview / rejectReview）。
3. ReviewCreateView：
   - onMounted/ handleCanteenChange 改为调用 loadDishesByCanteen 从后端实时获取该食堂的全部档口和菜品。
   - 修复手动选择食堂后档口下拉为空的问题。

## 影响范围
- 影响模块：管理员审核台、点评投稿页
- 影响接口：GET /reviews/admin、PUT /reviews/:id/audit、GET /canteens/:id/dishes
- 向前兼容：是，新接口仅前端调用

## 合并冲突处理指引
- 若 AdminAuditView.vue 冲突，确保 activePanel 包含 'review' 选项，reviewStore 已导入。
- 若 ReviewCreateView.vue 冲突，确保 handleCanteenChange 中调用 loadDishesByCanteen。
- 若 useReviewStore.js 缺失，复制 src/store/useReviewStore.js。
