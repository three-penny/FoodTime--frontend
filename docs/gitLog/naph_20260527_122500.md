# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-27 12:25:00
- **提交者**：opencode
- **Commit Hash**：TBD

## 提交信息
fix: 修复密码字段 trim 截断及邮箱大小写校验问题

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/views/auth/LoginView.vue | 修改 | 密码输入移除 v-model.trim，避免静默截断首尾空格 |
| src/views/auth/RegisterView.vue | 修改 | 同上；邮箱正则添加 /i 标志支持大写 BJTU |

## 变更详情
1. LoginView / RegisterView 密码输入框：将 v-model.trim 改为 v-model，保留密码原始内容。
   - 后端 auth_service 在 register 和 login 中统一执行 password.strip()，前后端修剪策略一致。
2. RegisterView 邮箱正则：/^\d+@bjtu\.edu\.cn$/ → /^\d+@bjtu\.edu\.cn$/i，支持用户输入大写 BJTU.EDU.CN。

## 影响范围
- 影响模块：用户注册、用户登录
- 影响接口：POST /auth/register、POST /auth/login
- 向前兼容：是

## 合并冲突处理指引
- 若 RegisterView.vue 冲突，确保 EMAIL_REGEX 包含 /i 标志，密码字段无双向绑定修饰符。
