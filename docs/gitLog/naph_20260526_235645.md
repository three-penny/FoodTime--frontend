# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-26 23:56:45
- **提交者**：naph130
- **Commit Hash**：9257ad8

## 提交信息
feat: 打通首页食堂选择到全部档口的前后端数据链路

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/components/home/CanteenCarousel.vue | 修改 | 导航改为 canteenDetail、rating 空值防御 |
| src/components/home/CanteenIntroGrid.vue | 修改 | 导航改为 canteenDetail |
| src/components/home/CanteenCarousel.spec.js | 修改 | 测试路由同步 |
| src/components/home/CanteenIntroGrid.spec.js | 修改 | 测试路由同步 |
| src/store/useCanteenStore.js | 修改 | setActiveCanteen 移除前置检查 |
| src/views/canteen/CanteenDetailView.vue | 修改 | 更新副标题、空状态、rating 防御 |
| src/views/dish/DishListView.vue | 修改 | 同步更新副标题、空状态、rating 防御 |
| src/components/canteen/CanteenStallCard.vue | 修改 | dish.rating 空值防御 |

## 变更详情
1. 首页轮播和介绍卡片导航从 dishList 改为 canteenDetail，补全食堂档案页入口。
2. CanteenDetailView 和 DishListView 更新副标题、新增无档口空状态提示。
3. CanteenStallCard 和 CanteenCarousel 增加 rating 空值防御 (?? 0)。
4. useCanteenStore.setActiveCanteen 移除 canteens 存在性前置检查。

## 影响范围
- 影响模块：首页食堂选择、食堂档案页、全部档口展示
- 影响接口：GET /api/v1/canteens/spots、GET /api/v1/canteens/:id/stalls
- 向前兼容：是

## 合并冲突处理指引
- 若 CanteenCarousel 路由导航冲突，保留 canteenDetail 作为目标。
- 若空值防御模式冲突，统一使用 `(value ?? 0).toFixed(1)` 风格。
