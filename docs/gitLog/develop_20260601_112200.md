# Git 提交日志

## 基本信息
- **分支名**：develop
- **提交时间**：2026-06-01 11:22:00
- **提交者**：NAPHthalene130
- **Commit Hash**：13d3b4f

## 提交信息
refactor(superadmin): 改密码弹窗由 prompt 改为自定义悬浮窗

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| src/views/superadmin/SuperadminView.vue | 修改 | 新增模态框模板、逻辑和样式；替换原有 prompt |

## 变更详情
1. 移除 `window.prompt` 调用，改为自定义 `modal-overlay` + `modal-card` 悬浮窗
2. 新增响应式状态 `passwordModal`（visible, target, password, error, submitting）
3. 输入框支持 Enter 提交，按钮有 loading 状态，错误提示内联显示
4. 点击遮罩层可关闭弹窗，确认后自动重置状态

## 影响范围
- 影响模块：超级管理员控制台（用户管理标签页）
- 向前兼容：是
