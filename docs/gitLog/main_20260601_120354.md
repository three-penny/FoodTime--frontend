# Git 提交日志

## 基本信息
- **分支名**：main
- **提交时间**：2026-06-01 12:03:54
- **提交者**：NAPHthalene130
- **Commit Hash**：abe9185

## 提交信息
feat(deploy): 添加 Dockerfile、nginx.conf 与 .dockerignore

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| Dockerfile | 新增 | 两阶段构建：node build → nginx serve |
| nginx.conf | 新增 | 代理 /api/ 到后端 + SPA fallback |
| .dockerignore | 新增 | 排除 node_modules、.git 等 |

## 变更详情
1. 两阶段构建：node:20-alpine 编译，nginx:stable-alpine 运行
2. nginx 配置：/api/* 反向代理到后端，/* 返回 SPA 并 fallback 到 index.html
3. 启用 gzip 压缩，限制请求体 10MB
4. .dockerignore 排除开发依赖文件，减小镜像体积

## 影响范围
- 新文件，无影响
